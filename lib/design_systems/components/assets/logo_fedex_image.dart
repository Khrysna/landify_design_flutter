import 'package:flutter/material.dart';

class LogoFedexImage extends StatelessWidget {
  const LogoFedexImage({super.key});

  @override
  Widget build(BuildContext context) {
    return Image.asset('assets/logo_fedex.png', height: 28);
  }
}
