import 'package:flutter/material.dart';

class LogoStrapiImage extends StatelessWidget {
  const LogoStrapiImage({super.key});

  @override
  Widget build(BuildContext context) {
    return Image.asset('assets/logo_google.png', height: 32);
  }
}
