import 'package:flutter/material.dart';

class IntegrationsImage extends StatelessWidget {
  const IntegrationsImage({super.key});

  @override
  Widget build(BuildContext context) {
    return Image.asset('assets/integrations.png');
  }
}
