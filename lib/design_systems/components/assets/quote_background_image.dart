import 'package:flutter/material.dart';

class QuoteBackgroundImage extends StatelessWidget {
  const QuoteBackgroundImage({super.key});

  @override
  Widget build(BuildContext context) {
    return Image.asset('assets/quote_background.png', height: 116);
  }
}
