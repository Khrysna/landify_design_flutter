import 'package:flutter/material.dart';

class LogoGoogleImage extends StatelessWidget {
  const LogoGoogleImage({super.key});

  @override
  Widget build(BuildContext context) {
    return Image.asset('assets/logo_google.png', height: 28);
  }
}
