import 'package:flutter/material.dart';

class AppStoreImage extends StatelessWidget {
  const AppStoreImage({super.key});

  @override
  Widget build(BuildContext context) {
    return Image.asset('assets/app_store.png', height: 40);
  }
}
