import 'package:flutter/material.dart';

class QuoteImage extends StatelessWidget {
  const QuoteImage({super.key});

  @override
  Widget build(BuildContext context) {
    return Image.asset('assets/quote.png', height: 16);
  }
}
