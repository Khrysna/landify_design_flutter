import 'package:flutter/material.dart';

class GooglePlayImage extends StatelessWidget {
  const GooglePlayImage({super.key});

  @override
  Widget build(BuildContext context) {
    return Image.asset('assets/google_play.png', height: 40);
  }
}
